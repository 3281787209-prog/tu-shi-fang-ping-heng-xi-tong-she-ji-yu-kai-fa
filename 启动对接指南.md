# 土石方平衡协同系统 · 启动与对接指南（用户版）

本文件只讲**你现在需要做的 3 件事**，让系统在你电脑上跑起来。

---

## 🎯 快速启动（你现在只需要看这一段）

### Windows 用户（推荐）

1. **打开 PowerShell**（Win + X → Windows PowerShell / 终端）
2. **进入项目目录**：
   ```powershell
   cd "d:\电脑管家迁移文件\xwechat_files\wxid_2h8njg1uzbd822_4ca5\msg\file\2026-08\开发\earthwork-balance-system"
   ```
3. **运行一键启动脚本**：
   ```powershell
   .\start-earthwork.ps1
   ```
4. 脚本会自动完成：
   - ✅ 安装 Python 依赖（只首次）
   - ✅ 初始化数据库 + 种子数据（23 个工况、15 个传感器、7 份表单、8 个地质图层、6 条告警…）
   - ✅ 启动后端 FastAPI：**http://localhost:8000/docs**
   - ✅ 安装前端 npm 依赖（只首次）
   - ✅ 启动前端 Vite：**http://localhost:5173/**（自动打开浏览器）
5. 用默认账号登录：

   | 用户名 | 密码 | 权限 |
   |--------|------|------|
   | admin | admin123 | 所有模块 + 系统管理 |
   | manager | manager123 | 审批、调度、看报表 |
   | engineer | engineer123 | 地质、监测、参数、3D 交互 |
   | user01 | user123 | 只读查看 |

### Linux / macOS 用户

```bash
cd earthwork-balance-system
chmod +x start-earthwork.sh
./start-earthwork.sh
```

---

## 🔧 手动分步启动（脚本失败时使用）

### 第一步：启动后端

```powershell
# 终端 1
cd earthwork-balance-system\backend
pip install -r requirements.txt        # 只首次
python -c "from app.init_db import main; main()"   # 只首次，初始化数据库
python -m uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload
```

启动后验证：浏览器打开 **http://localhost:8000/docs** → 看到 Swagger API 文档页面即为成功。

### 第二步：启动前端

```powershell
# 终端 2（开新窗口，终端 1 别关）
cd earthwork-balance-system\frontend
npm install            # 只首次，安装 node_modules
npm run dev            # 启动开发服务器，端口 5173
```

启动后浏览器会自动打开 **http://localhost:5173/**

---

## 🔗 前后端是怎么对接的？（原理说明）

### 开发模式（你现在用的）

```
 浏览器 http://localhost:5173
        │
        ▼
  Vite Dev Server
        │  命中 /api/* 请求
        ▼
  Vite 代理 ──────►  FastAPI  http://localhost:8000
  (vite.config.js
   server.proxy)
```

- **前端代码里**写 `request.get('/api/dashboard/summary')` — 看起来请求的是 5173
- **Vite 自动**把 `/api/*` 转发到 `http://localhost:8000/api/...`
- 所以**完全没有跨域（CORS）问题**，不需要做任何浏览器/Chrome 设置
- 配置文件：`frontend/vite.config.js` 里的 `server.proxy` 段

### 生产模式（你上传到 Render 之后）

两种方式，都已经在 render.yaml 里写好：

- **方式 A（默认，Blueprint）**：前端静态站 + 后端 API 分离部署  
  前端地址 https://earthwork-web-xxx.onrender.com  
  后端地址 https://earthwork-api-xxx.onrender.com  
  通过环境变量 `VITE_API_BASE_URL` 告诉前端后端的真实域名，前端代码直接发跨域请求，后端已配置 `CORS_ORIGINS=*`

- **方式 B（同源，更简单）**：把前端构建产物 `frontend/rebuild-dist/` 复制到 `backend/webroot/`，后端 FastAPI 直接同时托管 HTML + API  
  在 `backend/app/main.py` 里已经写好这段 SPA fallback 代码，部署后一个域名搞定。

---

## 📋 模块与数据真实来源

所有页面的数字、列表、图表数据都是真实的：

| 模块 | 前端路由 | 后端 API | 数据来源（种子初始化时创建）|
|------|----------|----------|---------------------------|
| 业务首页 Dashboard | `#/dashboard` | `/api/dashboard/summary`、`/form-trend`、`/earthwork-balance` 等 | 数据库 projects/forms/sensors/alerts 表聚合 |
| 表单审批 | `#/forms` | `/api/forms`（GET 列表 / POST 新建 / POST {id}/approve 审批） | 7 份示例表单（schedule_plan、地质勘察报告、变更申请、异常上报、参数计算、监测周报、土石方调配）|
| 地质信息 | `#/geology` | `/api/models/geology/layers`、`/boreholes`、`/stages` | 8 个地质图层 + 5 个钻孔 + 23 个开挖工况 + 每工况物理量指标 |
| 监测信息 | `#/monitoring` | `/api/monitoring/sensors`、`/overview`、`{id}/readings` | 15 个传感器（M01~M15：位移计/应力计/渗压计/测斜仪/锚杆测力计）+ 2184 条模拟监测读数 |
| 参数繁衍 | `#/params` | `/api/inference/*` + `/api/models/stages/{key}/metrics` | 23 工况位移/应力/孔隙水压力 291 条物理量指标反演计算 |
| 安全预警 | `#/alerts` | `/api/alerts/rules`、`/alerts`、`POST /check-all` | 7 条预警规则（位移阈值/应力阈值/速率阈值等）+ 6 条历史告警 |
| 系统管理 | `#/system`（admin 可见） | `/api/admin/users`、`/roles` 等 | 用户、角色、权限管理 |
| 3D 协同大屏 | `#/control-3d` | `/api/interactive3d/excavation/stages` + `/static/model_cache/*.vtp` | 工况列表 + 真实 VTP 多边形模型（VTK.js 渲染）|
| 3D 结构图层 | `#/layer-3d` | 同上 + `/api/models/geology/*` | 地质图层 + 模型切片叠加显示 |

---

## 🚀 常见问题

**Q1：浏览器打开前端一片空白？**  
→ 按 F12 打开控制台，看红色错误信息；99% 是后端没启动或端口被占用

**Q2：后端端口 8000 被占了怎么办？**  
→ 改启动命令端口：`python -m uvicorn app.main:app --port 8001`，同时改 `frontend/vite.config.js` 的代理 target 为 8001

**Q3：前端 5173 端口被占？**  
→ `npm run dev -- --port 5174` 或改 vite.config.js 的 `server.port`

**Q4：npm install 卡在下载不动？**  
→ 换国内镜像：`npm config set registry https://registry.npmmirror.com` 再重试

**Q5：Dashboard 卡片全是 0？**  
→ 通常是后端没启动，按 F12 检查 Network 里 /api 请求的状态码。如果是 401 → 重新登录；如果是 500 → 看后端终端的错误堆栈。

**Q6：3D 模型加载不出？**  
→ 检查 `frontend/public/model_cache/exac_1/full_model.vtp` 是否存在；F12 Network 看 .vtp 请求是否 200；如果 404 说明 model_cache 没拷贝到 public 目录。

---

## 📦 上传到 GitHub & 部署到 Render

详见本目录下：
- `README-DEPLOY.md` —— GitHub + Render 部署步骤
- `render.yaml` —— Render Blueprint 一键编排文件（双服务：earthwork-api + earthwork-web）

上传到 GitHub 后，登录 render.com → New → Blueprint → 选仓库 → 选 render.yaml → Apply Changes，10 分钟即可得到公网域名。
